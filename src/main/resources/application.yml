spring:
  application:
    name: produtosAPI
  datasource:
    url: jdbc:h2:mem:mydb
    username: nicolas
    password: 101208
  jpa:
    hibernate:
      ddl-auto: create              # ou update, create-drop
      show-sql: true                  # para ver os comandos SQL no console
      database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enable: true
      path: /h2-console